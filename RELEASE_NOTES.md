# Release Notes

Author-facing note: if the final anonymous Hugging Face repo uses a
different URL than `https://huggingface.co/datasets/anonymous-ai-exposure/jagged-global-economy`, rebuild this release with:

```bash
NEURIPS_DATASET_URL=<final_dataset_url> python3.11 submission_artifacts/build_neurips_ed_release.py
```

This file is informational; reviewers do not need to rebuild from raw
sources to inspect the release.
